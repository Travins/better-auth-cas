{
  "name": "better-auth-cas-repo",
  "version": "0.1.0",
  "private": true,
  "description": "Better Auth CAS plugin + integration skill docs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "plugin:install": "npm ci --prefix packages/better-auth-cas",
    "plugin:build": "npm run --prefix packages/better-auth-cas build",
    "plugin:test": "npm run --prefix packages/better-auth-cas test",
    "plugin:verify": "npm run plugin:install && npm run plugin:build && npm run plugin:test"
  },
  "dependencies": {}
}
